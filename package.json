{
  "name": "anchorpoint-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "bash scripts/deploy.sh",
    "verify": "node scripts/verify-seo.mjs",
    "validate:schema": "node scripts/validate-schema.mjs",
    "test:guardrails": "node scripts/test-guardrails.mjs",
    "check": "astro build && node scripts/verify-seo.mjs && node scripts/validate-schema.mjs && node scripts/test-guardrails.mjs"
  },
  "dependencies": {
    "astro": "^7.1.6"
  }
}
